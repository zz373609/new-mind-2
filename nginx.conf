server {
  listen 80;
  listen [::]:80;
  server_name shinemeditation.cn;
  
  location / {
    root /www/data;
    index index.html;
  }

  location /static {
      root /www/data/;
  }

  location /api/ {
      proxy_pass http://localhost:5030/;
  }
}
